name: Detect npm latest dsh

on:
  schedule:
    - cron: "17 */2 * * *"
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.detect.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - id: detect
        run: |
          ver="$(bash scripts/latest-dsh-rc.sh)"
          echo "version=${ver}" >> "$GITHUB_OUTPUT"
          if [ -n "$ver" ]; then
            echo "new latest: $ver"
          else
            echo "npm latest matches dsh.version"
          fi

  release:
    needs: detect
    if: needs.detect.outputs.version != ''
    uses: ./.github/workflows/release.yml
    with:
      version: ${{ needs.detect.outputs.version }}
    secrets: inherit
    permissions:
      contents: write
