version: '3'

vars:
  APP_NAME: "dsh-go"
  BIN_DIR: "bin"
  UPDATE_REPO: '{{.UPDATE_REPO | default ""}}'
  APP_VERSION: '{{.APP_VERSION | default ""}}'
  GOOS: '{{.GOOS | default OS}}'

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml

tasks:
  build:
    summary: Builds the application for the host OS
    cmds:
      - task: "{{.GOOS}}:build"

  package:
    summary: Packages a production build for the host OS
    cmds:
      - task: "{{.GOOS}}:package"

  package:update-zip:
    summary: Packages updater zip (and macOS DMG)
    cmds:
      - task: package:dist

  package:dist:
    summary: Packages updater zip; macOS also writes an ULMO DMG
    cmds:
      - task: '{{if eq OS "darwin"}}package{{else}}build{{end}}'
        vars:
          UPDATE_REPO: '{{.UPDATE_REPO}}'
          APP_VERSION: '{{.APP_VERSION}}'
      - bash ./scripts/package-dist.sh

  run:
    summary: Runs the application
    cmds:
      - task: "{{.GOOS}}:run"

  dev:
    summary: Builds with DEV=true and runs
    cmds:
      - task: build
        vars:
          DEV: "true"
      - task: run
